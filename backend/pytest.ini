[pytest]
minversion = 8.0
testpaths = tests
addopts = -ra --strict-markers
markers =
    unit: Fast, isolated tests with no DB/network/filesystem side effects
    integration: API and DB-backed tests using Alembic migrations

